# Development API
PORT=3000

# PostgreSQL persistence
# Never commit a live credential. Supply this through the local environment or secret manager.
DATABASE_URL=postgresql://dciecms:change-me@localhost:5432/dciecms
